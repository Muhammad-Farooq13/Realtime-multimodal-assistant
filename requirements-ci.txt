# CI-only: lean install without heavy ML packages (faster-whisper, torch, librosa)
# Whisper and openai are mocked in tests; only core types needed.

# --- Async core ---
aiofiles>=23.2.1
aiohttp>=3.9.1

# --- API ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.1
websockets>=12.0
python-multipart>=0.0.9
httpx>=0.27.0

# --- Audio (numpy only; libsndfile via apt in CI) ---
numpy>=1.26.2

# --- Vision (Pillow only; cv2 mocked) ---
Pillow>=10.2.0

# --- Monitoring ---
prometheus-client>=0.20.0
structlog>=24.1.0

# --- Config & utilities ---
pydantic>=2.6.3
pydantic-settings>=2.2.1
python-dotenv>=1.0.1
tenacity>=8.2.3

# --- Testing ---
pytest>=8.1.0
pytest-asyncio>=0.23.5
pytest-cov>=5.0.0
pytest-mock>=3.12.0
pytest-timeout>=2.3.1
respx>=0.20.2

# --- Linting & formatting ---
black>=24.3.0
ruff>=0.3.3
